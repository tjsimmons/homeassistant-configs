turn_playroom_off:
  sequence:
    - service: switch.turn_off
      entity_id:
        - switch.playroom_fan_lights_switch
        - switch.playroom_main_lights_switch
        - switch.playroom_tv_switch
    - service: fan.set_speed
      entity_id: fan.playroom_fan_level
      data:
        speed: medium
